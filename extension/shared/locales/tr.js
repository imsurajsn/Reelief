/**
 * Turkish (tr). Not translated yet — every key falls back to English
 * (shared/i18n.js), so the UI stays fully usable. Drop translations into
 * this object (see shared/locales/es.js for the full key list) to bring
 * Turkish online — no code change needed (PRD FR-36).
 *
 * Also give `meta.ordinal` a Turkish-appropriate form when translating
 * `overlay.titleN`.
 */

export default {};
